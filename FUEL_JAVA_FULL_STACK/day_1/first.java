package Day_1;

import java.util.*;

public class first {
    public static void main(String[] args) {

         Scanner sc = new Scanner(System.in);
   
    // Read the next line of input from the console as a String (presumably the user's name)
    String name= sc.nextLine();

    // Read the next int from the console (presumably the user's roll number)
    int rollno = sc.nextInt();

    // Read the next line of input from the console as a String (presumably the name of the user's college)
    String collegeName = sc.nextLine();

    // Read the next line of input from the console as a String (presumably the name of the user's city)
    String city = sc.nextLine();

    // Read the next line of input from the console as a String (presumably the user's branch of study)
    String branch = sc.nextLine();

    System.out.println("Name: " + name + "\nRoll Number: " + rollno + "\nCollege Name: " + collegeName + "\nCity: " + city + "\nBranch: " + branch);

    }
    // Create a Scanner object to read input from the console
   
    
}